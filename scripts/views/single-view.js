'use strict';

var app = app || {}

(function(module){ // start IIFE

  module.cardView.initIndexPage() {
    console.log('init Single Card Page'); 
    app.showOnly('single'); 
  };

})(app); // end IIFE